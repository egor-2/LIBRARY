# Paper Library

Maintainable collection of technical papers as Markdown. Papers are stored in subfolders, indexed in `records.csv`.

```bash
claude "add the Adam paper by Kingma and Ba" --allowed-tools Read Write Bash WebFetch WebSearch
```
